export interface CardInfo {
    count_all: number;
    count_in_work: number;
    count_ready: number;
    count_await: number;
    tariff: number;
    proposed_tariff: number;
    production_step__id: number;
}